---
import BrowserWindow from './BrowserWindow.astro';
---

<BrowserWindow url="http://voltrux.com/test-drive-form">
  <div class="test-drive-container">
    <div class="test-drive-form-col">
      <div class="form-header">
        <h2>Book Your Test Drive</h2>
        <p>Select your preferred model and time slot</p>
      </div>

      <div class="form-wrapper">
        <form id="form-testdrive" onsubmit="event.preventDefault(); alert('Test drive request submitted!');">
          <div class="form-group">
            <label for="firstName">First Name*</label>
            <input type="text" id="firstName" name="firstName" required />
          </div>

          <div class="form-group">
            <label for="lastName">Last Name*</label>
            <input type="text" id="lastName" name="lastName" required />
          </div>

          <div class="form-group">
            <label for="email">Email*</label>
            <input type="email" id="email" name="email" required />
          </div>

          <div class="form-group">
            <label for="phone">Phone</label>
            <input type="tel" id="phone" name="phone" />
          </div>

          <div class="form-group">
            <label for="location">Preferred Location*</label>
            <select id="location" name="location" required>
              <option value="" disabled selected>Select a location</option>
              <option value="downtown">Downtown Showroom</option>
              <option value="northside">Northside Dealership</option>
              <option value="eastside">Eastside Gallery</option>
            </select>
          </div>

          <div class="form-group">
            <label for="model">Preferred Model*</label>
            <select id="model" name="model" required>
              <option value="" disabled selected>Select a model</option>
              <option value="v8">Voltrux V-8</option>
              <option value="executive">Voltrux Executive</option>
              <option value="sport">Voltrux Sport</option>
              <option value="luxury">Voltrux Luxury</option>
            </select>
          </div>

          <div class="form-group">
            <label for="time">Preferred Time*</label>
            <select id="time" name="time" required>
              <option value="" disabled selected>Select a time</option>
              <option value="10:00">10:00 AM</option>
              <option value="11:00">11:00 AM</option>
              <option value="12:00">12:00 PM</option>
              <option value="13:00">1:00 PM</option>
              <option value="14:00">2:00 PM</option>
              <option value="15:00">3:00 PM</option>
              <option value="16:00">4:00 PM</option>
            </select>
          </div>

          <div class="form-group button-group">
            <button type="submit" class="cta-button">Book Now</button>
          </div>
        </form>
      </div>
    </div>

    <div class="test-drive-info-col">
      <div class="info-box why-voltrux">
        <h3>Why Test Drive a Voltrux?</h3>
        <ul>
          <li><span>✓</span>Experience our cutting-edge technology firsthand</li>
          <li><span>✓</span>Feel the power of our premium electric engines</li>
          <li><span>✓</span>Explore luxury features designed for comfort</li>
          <li><span>✓</span>Discover our revolutionary safety systems</li>
          <li><span>✓</span>Test our autonomous driving capabilities</li>
        </ul>
      </div>

      <div class="info-box other-info">
        <h3>Contact & Location Info</h3>
        <p>For any questions, please contact us at <a href="tel:+15551234567">(555) 123-4567</a> or visit any of our locations during business hours (9 AM - 8 PM, Mon-Sat).</p>
        <p>After submitting your request, a representative will contact you within 24 hours to confirm your appointment.</p>
      </div>
    </div>
  </div>
</BrowserWindow>

<style>
.test-drive-container {
  display: flex;
  gap: 2rem;
  padding: 2rem;
  background-color: #fff;
  border: 1px solid var(--vxt-border-color);
  border-radius: 8px;
  box-shadow: var(--vxt-shadow-medium);
}
.test-drive-form-col {
  flex: 2;
  min-width: 0;
}
.form-header {
  margin-bottom: 2rem;
}
.form-header h2 {
  font-size: var(--vxt-font-size-xxl);
  font-weight: 700;
  color: var(--vxt-primary-color);
  margin-bottom: 0.5rem;
}
.form-header p {
  font-size: var(--vxt-font-size-lg);
  color: var(--vxt-text-muted);
  margin: 0;
}
.form-wrapper {
  margin-bottom: 1rem;
}
#form-testdrive {
  display: flex;
  flex-direction: column;
  gap: 1rem;
}
.form-group {
  display: flex;
  flex-direction: column;
  gap: 0.5rem;
}
.form-group label {
  font-weight: 600;
  color: var(--vxt-primary-color);
}
.form-group input,
.form-group select {
  padding: 0.75rem;
  border: 1px solid var(--vxt-border-color);
  border-radius: 4px;
  font-size: var(--vxt-font-size-md);
  background-color: #fff;
}
.form-group select {
  appearance: none;
  background-image: url('data:image/svg+xml,%3csvg xmlns=\'http://www.w3.org/2000/svg\' fill=\'none\' viewBox=\'0 0 20 20\'%3e%3cpath stroke=\'%231A1A1A\' stroke-linecap=\'round\' stroke-linejoin=\'round\' stroke-width=\'1.5\' d=\'M6 8l4 4 4-4\'/%3e%3c/svg%3e');
  background-position: right 0.75rem center;
  background-repeat: no-repeat;
  background-size: 16px;
  padding-right: 2.5rem;
}
.button-group {
  margin-top: 1rem;
}
.cta-button {
  background-color: var(--vxt-cta-bg);
  color: white;
  padding: 12px 24px;
  font-weight: 700;
  border: none;
  border-radius: 50px;
  cursor: pointer;
  transition: all 0.3s ease;
  text-decoration: none;
  display: inline-block;
  font-size: var(--vxt-font-size-md);
  width: auto;
}
.cta-button:hover {
  background-color: var(--vxt-cta-hover);
  transform: translateY(-2px);
}
.test-drive-info-col {
  flex: 1;
  min-width: 300px;
  padding: 1.5rem;
  background-color: var(--vxt-neutral-color);
  border-radius: 8px;
}
.info-box {
  margin-bottom: 2rem;
}
.why-voltrux h3 {
  font-size: var(--vxt-font-size-xl);
  font-weight: 700;
  color: var(--vxt-primary-color);
  margin-bottom: 1rem;
  padding-bottom: 0.5rem;
  border-bottom: 2px solid var(--vxt-accent-color);
}
.why-voltrux ul {
  list-style: none;
  padding: 0;
  margin: 0;
}
.why-voltrux li {
  position: relative;
  padding-left: 1.5rem;
  margin-bottom: 0.75rem;
  color: var(--vxt-primary-color);
  font-size: var(--vxt-font-size-md);
}
.why-voltrux li span {
  position: absolute;
  left: 0;
  top: 0.25rem;
  color: var(--vxt-accent-color);
  font-weight: bold;
}
.other-info {
  font-size: var(--vxt-font-size-sm);
  color: var(--vxt-primary-color);
}
.other-info h3 {
  font-size: var(--vxt-font-size-lg);
  font-weight: 600;
  color: var(--vxt-primary-color);
  margin-bottom: 0.5rem;
}
@media (max-width: 768px) {
  .test-drive-container {
    flex-direction: column;
  }
  .test-drive-info-col {
    order: -1;
  }
}
</style>